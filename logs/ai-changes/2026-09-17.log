# AI Change Log — 2026-09-17

## MERGE-07 — Farmer GreenFin workflow

- Replaced the farmer-side legacy evidence／sustainability-proof workflow with the GreenFin digital profile.
- Added farmer navigation and six GreenFin work areas: overview, documents／OCR, green actions, green experience, four independent indicators and Data Health.
- Connected document upload, OCR field confirmation, normalization, verification and rule-based recalculation to the GreenFin APIs.
- Added `/api/greenfin/actions` for farmer-owned green action listing, creation and deactivation with shared GFES authentication, CSRF enforcement and evidence ownership checks.
- Kept the product boundary visible in the UI: GreenFin outputs are not credit scores or automated lending decisions.
- Updated rendered-HTML and farmer workflow coverage for the new GreenFin navigation and prohibited-result terminology.

### Verification

- `npx tsc --noEmit`: PASS.
- `npm run lint`: PASS (0 errors, 26 pre-existing non-blocking warnings).
- `npm test`: PASS (production build includes `/api/greenfin/actions`; 31 tests passed, 0 failed).
- Browser QA: PASS on the farmer account at `/farmer?section=greenfin`; the overview, document／OCR and green-action work areas rendered and remained usable after a clean development-server restart.

## MERGE-08 — Authorization and bank workflow

- Added farmer-controlled GreenFin authorizations with an explicit institution, purpose, data scope, start time, expiry time and revocation state.
- Validated the target institution against active GFES institution accounts and limited one authorization to at most 366 days.
- Added server-side authorization enforcement for institution access: institution identity, allowed data scope, active period and revocation state are checked for every detail request.
- Added GreenFin bank-case creation and an institution workspace that only exposes authorized experience, indicators and Data Health.
- Added `AUTHORIZATION_GRANTED`, `AUTHORIZATION_REVOKED` and `BANK_DATA_ACCESSED` audit writes, including actor and access context.
- Kept all GreenFin outputs separate and retained the explicit boundary that the information is supplemental and does not make lending decisions.

### Verification

- `npx tsc --noEmit`: PASS.
- `npm run lint`: PASS (0 errors, 26 pre-existing non-blocking warnings).
- `npm test`: PASS (production build includes authorization and bank-case APIs; 34 tests passed, 0 failed).
- Browser QA: PASS on `/institution?section=greenfin`; navigation, authorization controls, disclosure text and empty-state layout rendered correctly.

## MERGE-09 — Traceability, report and full regression

- Added an institution-only `/api/greenfin/reports` route that rechecks the active authorization before generating a report.
- Added an evidence-lineage package connecting each authorized result to its calculation trace, rule version, standardized record, OCR fields, verification／anomaly records and original document metadata.
- Added JSON report download from the institution workspace and recorded both bank access and report generation audit events.
- Cached the runtime schema bootstrap per D1 binding to prevent concurrent local requests from repeatedly executing the full schema setup.

### Verification

- `npx tsc --noEmit`: PASS.
- `npm run lint`: PASS (0 errors, 26 non-blocking warnings).
- `npm test`: PASS (production build includes `/api/greenfin/reports`; 37 tests passed, 0 failed).
- `npm run test:security:local`: PASS, including authentication, role isolation, CSRF, ownership, double-spend, duplicate reward, upload validation and session revocation checks.

## MERGE-10 — Public GitHub and deployment handoff

- Confirmed the public repository at `ossas0001/GFES-GreenFin-Eco-System` and pushed all completed merge gates to `main`.
- Added a Cloudflare deployment handoff that separates the recommended new-resource path from the explicitly approved existing-resource path.
- Clarified in the README that the existing GFES Pages URL does not yet contain the complete merged release.
- Verified Wrangler `4.92.0` and a successful deploy dry run; no production resources were changed.
