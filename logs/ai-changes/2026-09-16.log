# AI Change Log — 2026-09-16

## MERGE-01 — Project foundation

- Connected the GFES (`480b4b0`) and GreenFin (`4eac5c1`) Git histories with merge commit `d6770a3`.
- Renamed the merged project to **GFES (GreenFin Eco System)** and documented both upstream repositories.
- Added the merged product specification, rules, architecture, decisions and staged integration plan.
- Established the GreenFin product boundary: explainable sustainability data and indicators, not credit scoring, loan approval or pricing advice.
- Added project-foundation tests covering identity, required documents, rule version, D1/R2 architecture, shared authentication and upstream traceability.
- Repaired eight pre-existing React lint errors in `app/GreenPlatformDemo.tsx` by removing state synchronization effects and using stable initialization/remount boundaries.
- Created the public GitHub repository `ossas0001/GFES-GreenFin-Eco-System`.

## Verification

- `npm run lint`: PASS (0 errors, 26 non-blocking warnings).
- `npm test`: PASS (production build completed; 8 tests passed, 0 failed).
- Build warning retained for follow-up: one or more minified client chunks exceed 500 kB.
- Dependency audit reported 24 findings during installation (1 low, 6 moderate, 16 high, 1 critical). No forced automated upgrade was applied because dependency remediation must be reviewed for compatibility.

## Installation notes

- The first sandboxed `npm ci` attempt could not access the shared npm cache.
- Installation was completed with the project-local `.npm-cache`; the cache directory is ignored by Git.

## MERGE-02 — D1 core model and migrations

- Added D1/Drizzle models for farms, crops, original documents, extracted fields, standardized records, verification results, anomalies, versioned rule sets, green actions, experience transactions, four indicator results, Data Health results, authorizations, bank cases and GreenFin audit events.
- Kept GFES `profiles`, account controls and sessions as the only identity source; no parallel GreenFin user table was introduced.
- Generated versioned migration `0024_late_retro_girl.sql` and added matching runtime D1 bootstrap statements for local/demo environments.
- Added per-farmer document hash deduplication and indexes for evidence lineage, result retrieval, authorization lookup and audit traceability.
- Added an isolated SQLite migration test that creates all GreenFin tables and verifies key constraints.

### Verification

- `npm run db:generate`: PASS (44 total schema tables; migration generated).
- `npx tsc --noEmit`: PASS.
- `npm run lint`: PASS (0 errors, 26 pre-existing non-blocking warnings).
- `npm test`: PASS (production build completed; 12 tests passed, 0 failed).

## MERGE-03 — Document, OCR and normalization pipeline

- Added a farmer-only `/api/greenfin/documents` API using the existing GFES session and CSRF controls.
- Added PDF/JPEG/PNG/WebP/XLSX MIME, size and file-signature validation plus SHA-256 duplicate detection.
- Stores original GreenFin documents in the existing R2 binding under farmer-isolated keys.
- Added an OCR provider interface and a `MockOcrProvider`; every mock result is explicitly labelled `DEMO` or `SIMULATED`.
- Added OCR field confirmation/correction and deterministic field normalization for dates, amounts and hectares.
- Produces traceable standardized records and advances document status through `OCR_COMPLETED`, `FIELDS_CONFIRMED` and `NORMALIZED`.
- Writes `DOCUMENT_UPLOADED`, `OCR_COMPLETED` and `FIELD_CORRECTED` audit events.

### Verification

- `npx tsc --noEmit`: PASS.
- `npm run lint`: PASS (0 errors, 26 pre-existing non-blocking warnings).
- `npm test`: PASS (production build includes `/api/greenfin/documents`; 16 tests passed, 0 failed).
